[project]
name = "personaai"
version = "0.1.0"
requires-python = ">=3.10,<3.13"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv>=0.1.13"
]

[project.scripts]
server = "server.app:main"